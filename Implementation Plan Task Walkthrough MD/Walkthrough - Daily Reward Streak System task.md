# Tasks - Daily Reward Streak System

- [x] Update `AuthUser` class in `auth_provider.dart` to support daily claim columns
- [x] Update `WalletService` in `wallet_service.dart` to call the new Supabase RPC functions
- [x] Implement daily reward logic and guest fallbacks in `LevelProgress` and `LevelNotifier` (`level_provider.dart`)
- [x] Build the glassmorphic `DailyRewardDialog` UI with milestone chests and countdowns
- [x] Integrate automatic daily reward check and popup on lobby launch in `campaign_screen.dart`
- [x] Replace static daily reward card functionality in `exchange_overlay.dart` to open `DailyRewardDialog`
- [x] Run `flutter analyze` and resolve any compilation or type issues
- [x] Compile the final release APK to `f:\.gemini\antigravity\scratch\app-release.apk` and verify
