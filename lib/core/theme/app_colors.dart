import 'package:flutter/material.dart';

class AppColors {
  // ── Backgrounds ──────────────────────────────────
  static const deepNavy    = Color(0xFF030D1A);
  static const darkBlue    = Color(0xFF051925);
  static const midTeal     = Color(0xFF0A2535);

  // ── Accent / Glow ────────────────────────────────
  static const cyanGlow    = Color(0xFF00E5FF);
  static const tealAccent  = Color(0xFF00B4CC);
  static const tealMid     = Color(0xFF006980);
  static const cyanDark    = Color(0xFF004D5A);

  // ── Wave layers ──────────────────────────────────
  static const wave1 = Color(0xFF003344);
  static const wave2 = Color(0xFF004D5A);
  static const wave3 = Color(0xFF006672);
  static const wave4 = Color(0xFF008090);

  // ── Text ─────────────────────────────────────────
  static const textPrimary   = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF80D8FF);
  static const textMuted     = Color(0xFF3A6070);

  // ── Inputs ───────────────────────────────────────
  static const inputBg          = Color(0xFF0D1F2D);
  static const inputBorder      = Color(0xFF1A4050);
  static const inputBorderFocus = Color(0xFF00B4CC);

  // ── Status ───────────────────────────────────────
  static const error   = Color(0xFFFF5C5C);
  static const success = Color(0xFF00E676);

  // ── Holographic tube palette ──────────────────────
  static const tubeA = [Color(0xFF00FFFF), Color(0xFF0088FF), Color(0xFF8844FF), Color(0xFFFF44BB)];
  static const tubeB = [Color(0xFF44FFDD), Color(0xFF00CCFF), Color(0xFF5566FF), Color(0xFFFF88CC), Color(0xFFFFDD44)];
  static const tubeC = [Color(0xFF00FFAA), Color(0xFF00DDFF), Color(0xFF7744FF)];
}
